name: Conformance

on:
  schedule:
    # Run daily at 06:00 UTC
    - cron: '0 6 * * *'
  workflow_dispatch:

concurrency:
  group: conformance-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

env:
  CARGO_TERM_COLOR: always

jobs:
  conformance:
    name: Plow vs Knip
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - uses: ./.github/actions/setup-rust

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: '22'

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v4
        with:
          version: 9

      - name: Build plow
        run: cargo build --release -p plow-cli

      - name: Run conformance suite
        id: conformance
        run: |
          REPORT="$(./tests/conformance/run-all.sh \
            --plow-bin ./target/release/plow \
            --clone-dir /tmp/plow-conformance \
            --timeout 300 \
            2>conformance-stderr.txt)"
          echo "${REPORT}" > conformance-report.json

          # Validate report is valid JSON before extracting
          if ! python3 -c "import json; json.load(open('conformance-report.json'))" 2>/dev/null; then
            echo "::warning::Conformance report is not valid JSON"
            exit 1
          fi

          # Extract summary values for the step summary
          python3 -c "
          import json
          with open('conformance-report.json') as f:
              r = json.load(f)
          s = r['summary']
          with open('$GITHUB_OUTPUT', 'a') as out:
              out.write(f\"plow_total={s['plow_total']}\n\")
              out.write(f\"knip_total={s['knip_total']}\n\")
              out.write(f\"agreed={s['agreed']}\n\")
              out.write(f\"plow_only={s['plow_only']}\n\")
              out.write(f\"knip_only={s['knip_only']}\n\")
              out.write(f\"agreement_pct={s['agreement_pct']}\n\")
          "

          # Write custom JSON for benchmark-action tracking
          # Per-project agreement rates + overall
          python3 -c "
          import json
          with open('conformance-report.json') as f:
              r = json.load(f)
          data = [
            {'name': 'Agreement Rate', 'unit': '%', 'value': float(r['summary']['agreement_pct'])},
            {'name': 'Agreed Issues', 'unit': 'issues', 'value': int(r['summary']['agreed'])},
            {'name': 'Plow Total', 'unit': 'issues', 'value': int(r['summary']['plow_total'])},
            {'name': 'Knip Total', 'unit': 'issues', 'value': int(r['summary']['knip_total'])},
          ]
          for name, proj in sorted(r.get('projects', {}).items()):
            data.append({'name': f'{name} Agreement', 'unit': '%', 'value': float(proj['agreement_pct'])})
          print(json.dumps(data, indent=2))
          " > conformance-bench.json
        # Never fail -- this is informational
        continue-on-error: true

      - name: Store conformance metrics
        if: always() && steps.conformance.outcome != 'skipped'
        uses: ./.github/actions/store-benchmark
        with:
          name: Plow Conformance
          tool: customBiggerIsBetter
          output-file-path: conformance-bench.json
          data-dir: conformance
          alert-threshold: '130%'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          continue-on-error: 'true'

      - name: Post step summary
        if: always()
        env:
          PLOW_TOTAL: ${{ steps.conformance.outputs.plow_total }}
          KNIP_TOTAL: ${{ steps.conformance.outputs.knip_total }}
          AGREED: ${{ steps.conformance.outputs.agreed }}
          PLOW_ONLY: ${{ steps.conformance.outputs.plow_only }}
          KNIP_ONLY: ${{ steps.conformance.outputs.knip_only }}
          AGREEMENT_PCT: ${{ steps.conformance.outputs.agreement_pct }}
        run: |
          # Check if report exists and contains valid JSON
          if [[ ! -f conformance-report.json ]] || ! python3 -c "import json; json.load(open('conformance-report.json'))" 2>/dev/null; then
            {
              echo "## Conformance Test"
              echo ""
              echo "Conformance suite did not produce a valid report. Check the logs for errors."
            } >> "$GITHUB_STEP_SUMMARY"
            if [[ -f conformance-stderr.txt ]]; then
              {
                echo ""
                echo '```'
                cat conformance-stderr.txt
                echo '```'
              } >> "$GITHUB_STEP_SUMMARY"
            fi
            exit 0
          fi

          {
            echo "## Conformance Test: Plow vs Knip"
            echo ""
            echo "| Metric | Count |"
            echo "|--------|-------|"
            echo "| Plow issues | ${PLOW_TOTAL} |"
            echo "| Knip issues | ${KNIP_TOTAL} |"
            echo "| Agreed | ${AGREED} |"
            echo "| Plow-only | ${PLOW_ONLY} |"
            echo "| Knip-only | ${KNIP_ONLY} |"
            echo "| **Agreement** | **${AGREEMENT_PCT}%** |"
            echo ""
            echo "### Per Project"
            echo ""
            python3 -c "
          import json
          with open('conformance-report.json') as f:
              r = json.load(f)
          projects = r.get('projects', {})
          if projects:
              print('| Project | Plow | Knip | Agreed | Agreement |')
              print('|---------|--------|------|--------|-----------|')
              for name, s in sorted(projects.items()):
                  print(f\"| {name} | {s['plow_total']} | {s['knip_total']} | {s['agreed']} | {s['agreement_pct']}% |\")
          "
            echo ""
            echo "### By Issue Type"
            echo ""
            python3 -c "
          import json
          with open('conformance-report.json') as f:
              r = json.load(f)
          print('| Type | Plow | Knip | Agreed | Agreement |')
          print('|------|--------|------|--------|-----------|')
          for t, d in sorted(r['by_type'].items()):
              print(f\"| {t} | {d['plow_count']} | {d['knip_count']} | {d['agreed']} | {d['agreement_pct']}% |\")
          "
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: conformance-report
          path: |
            conformance-report.json
            conformance-stderr.txt
          retention-days: 30
