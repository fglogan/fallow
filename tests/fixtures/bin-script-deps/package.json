{
  "name": "bin-script-deps",
  "main": "src/index.ts",
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@j178/prek": "^0.2.0",
    "is-ci": "^4.0.0",
    "publint": "^0.3.0"
  },
  "scripts": {
    "lint": "publint --strict && attw --profile esm-only --pack .",
    "prepare": "is-ci || bun --bun prek install"
  }
}
